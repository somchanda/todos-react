import React, { Component } from 'react'

export class About extends Component {
    render() {
        return (
            <React.Fragment>
                <h1>About us</h1>
                This is to do application use with React.
            </React.Fragment>
        )
    }
}

export default About
